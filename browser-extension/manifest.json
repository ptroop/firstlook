{
  "manifest_version": 3,
  "name": "First Look — review-first form fill",
  "version": "0.1.0",
  "description": "Fill supported public application fields from a local profile. Never submits, uploads, or handles passwords.",
  "permissions": ["activeTab", "scripting", "storage"],
  "action": {
    "default_title": "First Look form fill",
    "default_popup": "popup.html"
  }
}
